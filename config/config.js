// # config.js
// config.js is the central place for us to have all the required
// configurations loaded. Here we add every parameter that can & should be configured.
// Right now, as you can see, we are having only usergridClientOptions.
exports.usergridClientOptions = usergridClientOptions = {
    orgName: 'r8k',
    appName: 'sandbox'
};